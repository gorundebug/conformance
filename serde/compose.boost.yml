services:
  cpp-build:
    image: servicelib-conformance-boost-cpp-example-build:local
    volumes:
      - canonical-cpp-conan2:/conan

volumes:
  canonical-cpp-conan2:
    name: cppexample_cpp-conan2
