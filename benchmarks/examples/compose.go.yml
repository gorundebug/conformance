services:
  inventoryservice:
    environment:
      GOMAXPROCS: ${BENCHMARK_SERVICE_CORES}
      DEBUG: "0"

  orderservice:
    environment:
      GOMAXPROCS: ${BENCHMARK_SERVICE_CORES}
      DEBUG: "0"
      ORDER_PROCESSED_ENABLED: "false"
