services:
  grpc-probe:
    image: ${GO_TOOLCHAIN_IMAGE:?GO_TOOLCHAIN_IMAGE is required}
    entrypoint: [/scenario-artifacts/grpc-probe]
    working_dir: /
    volumes:
      - ${SERVICELIB_SCENARIO_ARTIFACTS_DIR}:/scenario-artifacts:ro
    networks: [app_net]
    profiles: [conformance]
